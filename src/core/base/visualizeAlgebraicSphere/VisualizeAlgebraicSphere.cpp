#include <VisualizeAlgebraicSphere.h>

ttk::VisualizeAlgebraicSphere::VisualizeAlgebraicSphere() {
  // inherited from Debug: prefix will be printed at the beginning of every msg
  this->setDebugMsgPrefix("VisualizeAlgebraicSphere");
}
